<section id="hero" class="hero-section d-flex align-items-center position-relative overflow-hidden">
    <!-- Right side image for desktop -->
    <div class="position-absolute top-0 bottom-0 end-0 w-50 d-none d-lg-block">
        <img src="/assets/img/perro_shiba.jpeg" alt="Perro feliz en Bark & Co." class="hero-dog-img-split" />
    </div>

    <div class="container-fluid px-0 position-relative z-1">
        <div class="row g-0 align-items-center">
            <div class="col-lg-6 d-flex justify-content-center align-items-center">
                <div class="hero-content-wrapper text-center text-lg-start py-1 py-lg-5">
                    <h1 class="hero-title mb-3 font-decorative text-gradient">HOSPEDAJE CANINO<br />EN SAN PEDRO</h1>
                    <p class="lead mb-3 fs-5" style="color: var(--primary-color);">Tus vacaciones empiezan cuando dejas a tu perro con <br /> nosotros. Las de tu perro, cuando entra a Bark & Co.</p>
                    <p class="lead mb-4 fs-4" style="color: var(--primary-color);">¡Cupos limitados para este verano!</p>
                    <div class="d-flex justify-content-center justify-content-lg-start">
                        <a href="#booking" class="btn btn-accent-green btn-hero fw-bold shadow-sm">Reserva su lugar para verano →</a>
                    </div>
                </div>
            </div>

            <!-- Mobile dog image -->
            <div class="col-12 text-center mt-5 d-lg-none px-4">
                <img src="/assets/img/perro_shiba.jpeg" alt="Perro feliz en Bark & Co." class="img-fluid rounded-4 shadow-sm hero-dog-img-mobile" />
            </div>
        </div>
    </div>
</section>

<style>
    .hero-section::before {
        display: none !important;
    }
    .hero-section {
        padding-top: 10px !important; /* Espacio para barra de navegación en móviles */
        padding-bottom: 60px !important;
        min-height: 100vh;
    }
    @media (min-width: 992px) {
        .hero-section {
            padding-top: 110px !important; /* Espacio para barra de navegación en desktop */
            padding-bottom: 80px !important;
        }
    }
    .hero-dog-img-split {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }
    .hero-dog-img-mobile {
        max-width: 100%;
        max-height: 350px;
        object-fit: cover;
    }
    .hero-content-wrapper {
        width: 100%;
        padding-left: 10%;
        padding-right: 10%;
    }

    @media (min-width: 1700px) {
        .hero-content-wrapper {
            padding-left: 12%;
            padding-right: 12%;
        }
    }

    .hero-title {
        line-height: 0.85 !important;
    }
    .btn-hero {
        font-size: 0.85rem !important;
        padding: 0.5rem 1.1rem !important;
    }
    @media (min-width: 768px) {
        .btn-hero {
            font-size: 1rem !important;
            padding: 0.8rem 1.6rem !important;
        }
    }
</style>
