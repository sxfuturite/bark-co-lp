---
---
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Gracias | Bark & Co</title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&display=swap" rel="stylesheet">

    <!-- Custom CSS -->
    <link rel="stylesheet" href="/assets/css/main.css">

    <style>
        .body-gracias {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background-color: var(--primary-color, #1a1a1a);
        }
        .logo-gracias {
            max-width: 250px;
            margin-bottom: 2rem;
        }
        .btn-cta.white {
            background-color: white;
            color: var(--primary-color, #1a1a1a);
            padding: 12px 30px;
            border-radius: 50px;
            text-decoration: none;
            font-weight: bold;
            display: inline-block;
            transition: all 0.3s;
        }
        .btn-cta.white:hover {
            opacity: 0.9;
            transform: translateY(-2px);
        }
    </style>
</head>

<body class="body-gracias">
    <div class="container gracias">
        <div class="row">
            <div class="col-md-12 text-center text-white">
                <img src="/assets/img/logo_BarknCo.png" class="logo-gracias" alt="Bark & Co Logo">
                <h5 class="mt-5 fs-2 font-decorative" style="color: var(--secondary-color);">
                    ¡Muchas <span class="text-white">gracias</span> por contactarnos!
                </h5>
                <p class="mt-4 mb-5 fs-5" style="color: rgba(255,255,255,0.8);">
                    Recibimos tus datos, en breve <br>
                    un asesor se comunicará contigo.
                </p>
                <a class="btn-cta white" href="/">IR A INICIO</a>
            </div>
        </div>
    </div>
</body>
</html>
